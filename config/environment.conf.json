/*
  CleverStack Angular Environment Setup Configuration.
  This config can be used to install any missing dependencies you might have.
*/
{
  "environment": {

    /*
      Operating System required for setup:
        - latest install of phantomjs
        - latest install of selenium 2.0 webdriver
        - latest install of chrome driver
      For more info see: https://github.com/clevertech/cleverstack-angular-seed#installing-selenium-webdriver--protractor
    */
    "OS": "win", // win, mac, linux
    "OS_TYPE": "32" // 32, 64

  }
}
